# LaVIS 개선안 — UDInspect(2D-Verifier) 참조 다각도 검토 (2026-09-08)

검토 방법: UDInspect 저장소(디자인 시스템·강건성/HFE 리뷰·PRD·가치 평가)와 LaVIS(UI·엔진·이력)를 6개 판독으로
분석 → 5개 관점(패밀리 디자인 / HFE·사용 오류 방지 / 최고의 커스터마이징 / 강건성·데이터 무결성 / 품질기록
워크플로) 제안자가 61건 제안 → 종합 편집 24건(A 13 · B 8 · C 3) → 항목별 반박 검증(중복·규칙 위반 / 실현성·해악)
37건 판정, 반박 1건(A-13: 검증 시점에 이미 구현되어 '중복'으로 판정 — 잔여분인 문서 토큰 존재 테스트만 추가). A 그룹은 이번 라운드에 구현했고 B/C는 아래에 제안으로 남긴다.

## 1. 이번 라운드 구현 (A 그룹)

| ID | 제목 | 핵심 |
|---|---|---|
| A-01 | 뷰 상태 헬퍼 자기 재귀 수정 + 소스 규약 테스트·CI 가드 | 검토 중 발견된 스택 오버플로 결함 즉시 수정, 재발 방지 테스트 |
| A-02 | 공용 대화상자 규범 | `Dialogs.Confirm`(기본 '아니오')·`ChoiceDialog`(동작을 말하는 버튼)·모든 모달 Esc·정보성 팝업 강등 |
| A-03 | 설정 값 검증 Core 계층 | `SettingRanges` 범위표 + `AppConfig.Normalize`(범위 clamp·enum·배열 검증·보정 통보) + `PathRules` |
| A-04 | GS1 관대 파싱 + OCR 폴백 차단 | (91)/(8012) 등 미등록 AI가 있어도 AI(01)·(10) 대조, 해석 불가 바코드는 '확인 필요' |
| A-05 | 자동 저장 중복 방지 | `InspectionOutcome.Signature()`로 같은 판정은 재저장 안 함, 수동 재저장 확인, 파일 덮어쓰기 금지 |
| A-06 | 테마 토큰 2차 정합 | 인라인 색·크기 토큰화, 플래시 피드백(`UiFx`), `ThemeTokenTests` |
| A-07 | 판정·진행 시각 체계 | 배지 사유줄(`InspectionSummary.Describe`, 저장 이미지와 동일 문구), 슬롯 5상태(✓/!/?·저장 띠), 범례 칩 |
| A-08 | 툴바 위계·브랜딩·빌드 식별 | DockPanel 툴바·구분선, About 오버레이, 상태바 버전, CI `InformationalVersion`·SHA-256·trx, `app_version` |
| A-09 | 설정 입력 검증 가시화 | 붉은 배경·범위 툴팁·저장 차단·탭 전환, 저장 경로 검증·폴백·비차단 접근 확인, 저장 실패 원인별 안내 |
| A-10 | 내보내기 무결성 | `CsvUtil`(="값" 보호·BOM), 20열 `InspectionCsv`(미검사 행 포함), `ExportGuard`(파일 잠김·권한), 내보내기 전 확인 |
| A-11 | 영역 상시 표시·등록 UX | 등록 영역·양식 영역 점선+칩, 등록 모드 배너·Esc·연속 등록, 재등록 시 교체/추가 선택 |
| A-12 | 키보드 전용 흐름 | Ctrl+O/S, F5, N(다음 확인 대상), Ctrl+L/F, Ctrl+Shift+L, Esc, 포커스 순서·접근성 이름 |
| A-13 | 문서 2차 | family_design §7~§11, README 규제 위치·검사 규칙·커스터마이징, 체크리스트 V-00~V-09 |

사전 구현(계획 수립 전에 확정된 항목): 패밀리 디자인 1차(테마·상태바 3단계·미저장 표시·종료 확인·아이콘),
프리셋 번들(`.lavispreset`)·원자적 저장·스키마 버전, LOT 미매칭 경고·'확인 필요' 강제, 뮤텍스·app.log.

## 2. 다음 라운드 제안 (B 그룹)

| ID | 제목 | 요지 | 효과 | 규모 |
|---|---|---|---|---|
| B-01 | 저장 계층 무결성 | 학습 저장소 7종 원자 저장·저장 실패 통보·.bak 복구, 이력 DB `user_version` 마이그레이션·`VACUUM INTO` 백업 | 정전·동시 쓰기에도 학습 데이터·이력 보존 | L |
| B-02 | OCR 파이프라인 강건화 | 캐시 키에 엔진 서명, '다시 OCR', AWS 타임아웃·재시도·스로틀 백오프·취소, 재OCR 과금 사전 확인, 호출 계수, 렌더 픽셀 예산 | 과금·멈춤·오래된 캐시 사고 방지 | L |
| B-03 | 검사 세션 자동 저장·복구 + Undo | 크래시·PDF 재열기 시 수동 LOT·규격·저장 상태 복원, 영역·교정·병합·학습 되돌리기(Ctrl+Z) | 작업 손실·오등록 복구 | L |
| B-04 | 규격·목록 생성 규칙 커스터마이징 | 규격 추가·복제·삭제·날짜 포맷 UI, strftime 전 토큰, BSC/일본 하드코딩 제거, 컬럼 매핑(헤더명) UI, 국가별 규칙 표, 미등록 REF/GTIN 직접 입력 | JSON 편집 없이 신제품군 대응 | L |
| B-05 | 필드 규칙 통합 스키마 | 필드별 별칭·매칭 모드(부분/정확/단어/정규식)·허용 오차·날짜 입력 형식·제외어, 저신뢰 매치 가시화, 유효기간 만료 검사 | 라벨 표기 다양성·거짓 합격 감소 | L |
| B-06 | 운영 데이터 커스터마이징 | 파일명 토큰 규칙·날짜/LOT 폴더, 프리셋 라이브러리(재시작 없는 전환·dirty 감지), 기준정보 네트워크 원본→로컬 캐시 | 라인 전환 1클릭, 사내 파일 체계 준수 | L |
| B-07 | 설정 창·툴바 편집기 2차 | 설정 검색·도움말·기본값 비교/부분 초기화, 색 견본 피커, 단축키 편집, 내보내기 열 구성, 검사 탭 '옵션 ▾' 즉시 저장 패널 | 40개+ 설정의 탐색 부담 해소 | L |
| B-08 | 규제 위치·검증 문서 체계 | LaVIS판 PRD(REG-01~05), TOOL_VALIDATION(요구↔테스트↔실기 추적표), VERIFICATION_CHECKLIST 서명 양식 | UDInspect와 동일한 간이 검증 트랙 | M |

권장 순서: B-01 → B-02 → B-04/B-05 → B-06 → B-03 → B-07 → B-08 (B-08은 C-01 결정 후).

## 3. 사용자 판단이 필요한 제안 (C 그룹)

| ID | 제목 | 판단 사항 |
|---|---|---|
| C-01 (일부 구현 2026-09-09) | 검사자 최종 판정 레이어 — (b)(c)는 '검사자 확인 합격 처리'로 구현(결과 목록 창, 사유 필수, CSV `INSPECTOR/FINAL`, 이력 `inspector_*`, 저장 이미지 표기). (a) 서명 칸·(d) 파일명 접미 변경은 미결정 | 자동 판정을 '참고'로 두고 검사자가 P/F·부적합 사유·검사자·검사일을 기록하는 UDInspect REG-03 모델을 도입할지. (a) 검사 성적서에 검사자 서명 칸이 필요한지 (b) '확인 필요' 페이지를 검사자가 P로 확정하는 흐름을 허용할지 (c) 이력에 검사자 이름을 남길지 (d) 저장 파일명 접미를 자동 판정(_Passed/_Check)에서 검사자 판정(_OK/_NG)으로 바꿀지 |
| C-02 | 라벨 검사 성적서 PDF | C-01 채택 시: A4 성적서(요약·대조표·주석 이미지 부록·결재란)·양식번호/개정·회사 로고·빈 양식·이력 탭에서 재출력. 양식 번호와 결재란 구성이 필요 |
| C-03 | 접근성·현지화 준비 | 표시 크기 100/115/130/150% 모드(DynamicResource 전환)와 UI 문자열 리소스 분리(영문). 검사대 거리·저시력 요구와 해외 사업장 배포 계획이 실제로 있는지 |

## 4. 검토에서 제외한 것

- 자동 판정 제거, 기본 필드 확장, OCR 우선 GTIN, 좌표 입력식 영역 등록, 이형지 기준 기울기 보정, 타이틀 변경, 다크 테마·커스텀 템플릿 — 사용자 확정 규칙 또는 패밀리 디자인 원칙과 충돌.
- 전자서명·감사추적·로그인 — '검사 보조 도구' 위치(GAMP 위험 낮음)와 충돌. C-01은 그 범위 안에서의 최소 형태.

## 5. UDInspect 역이식 라운드 (별도 저장소)

LaVIS에서 먼저 생긴 장치 중 UDInspect에도 유효한 것을 되가져온 변경분을 2D-Verifier 저장소의 브랜치
`claude/lavis-family-insights`(커밋 메시지 `[skip ci]` — 브랜치 푸시만으로 latest-build 롤링 릴리스가 갱신되지 않게 함)에 푸시했고,
같은 내용을 `docs/udinspect_family_round.patch`로도 보관했다. 내용: Theme.xaml 토큰화(인라인 색 56곳), 상태바 경고 5초 고정,
About 오버레이·빌드 식별(InformationalVersion·SHA-256 BUILD_INFO.txt), 설정 백업/복원, 소스 규약 테스트(SourceGuardTests),
`docs/LAVIS_FAMILY_ROUND.md`. 적용: 2D-Verifier에서 해당 브랜치를 기본 브랜치에 병합(PR)하면 Windows CI가 테스트·빌드한다.
